Phase 2 media deletion. Red: 2/2 installed-runtime tests exposed suppressed filesystem and database failures. Green: 43 focused tests passed. File cleanup now propagates before DB deletion, leaving a retry target; subtype/shared Media deletion is one synchronous SQLite transaction with verified cascades and rollback. Strict slice typecheck and diff check clean; no new changed-line lint findings.
