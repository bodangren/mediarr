Phase 5 findings residual: a single S01E01E02 payload linked all torrent episode IDs but imported only episodeNumbers[0], leaving completion guard permanently incomplete. Red: 3 failures/14 passes proving no all-ID update and mutation on missing/mismatch. Green: full ImportManager/importGuard family 58/58; strict server typecheck; no new lint violations; diff check. One-file multi-episode preflight validates every linked episode and exact parsed set before mutation, organizes once, updates all paths with count verification, creates one canonical variant under current unique(mediaType,path) schema, emits/notifies once, and runs each episode hook. Source-regex pack tests replaced by stateful callback-driven behavior.
