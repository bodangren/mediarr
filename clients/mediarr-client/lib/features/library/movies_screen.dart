import 'package:flutter/material.dart';

/// Movies library browsing screen.
class MoviesScreen extends StatelessWidget {
  const MoviesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Movies Library'),
    );
  }
}
