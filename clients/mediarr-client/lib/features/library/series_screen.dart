import 'package:flutter/material.dart';

/// TV Series library browsing screen.
class SeriesScreen extends StatelessWidget {
  const SeriesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Series Library'),
    );
  }
}
