Task: Write Tests and Implement Torrent API endpoints (internal logic).
- Added getTorrentsStatus and getTorrentStatus to TorrentManager to return standardized status objects.
- Added mapping logic to convert BigInt fields to strings for JSON compatibility.
- Created tests/torrent-api.test.js to verify data structure and retrieval.
