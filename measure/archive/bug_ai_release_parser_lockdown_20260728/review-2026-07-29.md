Both gates re-verified independently: `tsc -p server/tsconfig.json --noEmit` → exit 0, and the four parser suites → 181 passed / 0 failed.

# Review: `bug_ai_release_parser_lockdown_20260728`

## 1. Summary of changes

22 commits, 2026-07-28 → archived in `88807678`. Track spans three defects and two additions:

| Area | Change |
|---|---|
| Batch attribution | `parseBatch()` return contract changed to `(ParsedReleaseWithScore \| null)[]` of `titles.length`. New exported `assignBatchSlots()` (`ReleaseParser.ts:273`) places results by model-echoed 1-based index; missing/duplicate/out-of-range → `null`. Index-less responses fall back to a strict length check. `MediaSearchService.ts:580` positional zip replaced with slot-wise assignment |
| Model default | `minimax/minimax-m2.7` → `google/gemini-2.5-flash-lite` (`acc62f5`) → `openrouter/pareto-code` router (`6437fe95`, FR-4 amended mid-track at user's direction) |
| Timeouts | Three hardcoded `AbortSignal.timeout` literals → `resolveReleaseParserRuntimeConfig()`; defaults 30s/60s/90s, env-overridable, non-positive values rejected |
| Cost dial (FR-8) | `min_coding_score` sent explicitly via `extraBody`, default 0 (measured floor), `+0.10` per retry |
| Loud failure (FR-9) | `ParserDegradation` union, `onReleaseParserDegraded()` observer, ≥60%-of-deadline slow-call warning, published as `parser:degraded` from `main.ts:214` |
| Concurrency (FR-7) | Serial promise chain → semaphore, default 4 |
| Prompt fix | `BATCH_PROMPT` was missing the `title` and `year` field rules that `PARSE_PROMPT` always had; `temperature: 0` added to all three call sites (was never set) |
| Tests/fixtures | 48-entry golden set + 55 regex-fallback assertions, `ReleaseParserProvider.test.ts` (73 tests), `MediaSearchService.batchAlignment.test.ts`, router/escalation/degradation tests |
| Tooling | `scripts/benchmark-release-parser.ts`, env-gated on `RELEASE_PARSER_BENCHMARK`, `npm run benchmark:parser` |

## 2. Spec alignment

| FR | Status | Note |
|---|---|---|
| FR-1 golden set | **Met** | 48 entries, all required categories, `regexFallbackExpected` on each, asserted at module load |
| FR-2 no mis-attribution | **Met** | Every uncertain case leaves `null`; the asymmetry is documented at the code site and at the call site |
| FR-3 configurable timeouts | **Met** | Includes the known-fast-model startup warning, memoised once per model |
| FR-4 (amended) router default | **Met** | `openrouter/pareto-code`, dial explicit, deadlines raised, `.env.example` updated |
| FR-5 live benchmark | **Partial** | Per-field accuracy, latency, validity and alignment all reported; **cost is not** — the doc states token usage isn't reachable through `parseBatch()` and declines to estimate. Spec asked for cost |
| FR-6 provider coverage | **Met** | 100% branch per Phase 6; 73 tests pass |
| FR-7 bounded concurrency | **Met** | Semaphore is correct — every `release()` wakes exactly one waiter and re-checks the bound, so no lost-wake |
| FR-8 escalation | **Met** | |
| FR-9 loud degradation | **Partial** | `parse()` and `parseBatch()` are fully instrumented. **`parseFiles()` is not** (`ReleaseParser.ts:663`): it still swallows errors into a bare `console.log('[parseFiles] error after …', err)`, emits no `parser:degraded`, no timeout classification, no slow-call warning — the same silent-degradation shape the track exists to remove |
| NFR-1 offline | Met | |
| NFR-2 <$2/month | **Unverified, likely missed** | See risks |
| NFR-3 regex works AI-less | Met | |

Acceptance criteria: 1, 2, 5, 6, 7, 8, 9 met (8 and 9 re-verified above). **AC-3 is stale** — it still reads "the default model resolves to `google/gemini-2.5-flash-lite`", never amended alongside FR-4; the shipped default and its test assert `openrouter/pareto-code`. The archived spec now contradicts itself. **AC-4 is not met as written**: it requires deadlines ≥3× measured p100, but the recorded benchmark's slowest batch is 28,612ms against a 60,000ms deadline — 2.1×.

## 3. Code quality observations

Strong overall. The comments carry *why* rather than *what*, the cost/latency tables are pinned in source next to the constants they justify, and the anti-regression comment at `MediaSearchService.ts:580` names the exact failure mode. Specific notes:

- **Recorded accuracy figures disagree three ways.** `benchmark-results.md` (the artifact) says title 87.5% (42/48); `plan.md:124` and `tech-debt.md:28` both say 91.7% (44/48); commit `6437fe95`'s message and the first version of the results file said 97.9% (47/48). `ce821956` overwrote 97.9→87.5 and 85.4→100 for `year`, so these are separate runs, not a typo — meaning **the oracle is not reproducible at `temperature: 0`**, because the router picks a different backing model per run. Nothing recorded in the track acknowledges that; the 91.7% in tech-debt cannot be reproduced by re-running the script.
- `_parseSingle` (`ReleaseParser.ts:503`) takes `model` from `attemptConfig` but `providerOptions` from the outer `aiConfig`. Harmless today (both `{openrouter:{}}`), latent if the two ever diverge.
- `parseFiles()` repeats the same 8-line regex-fallback object literal three times; the `regexResults()` closure right above it already does this.
- `eventHub.publish(event: string, …)` is untyped, so `parser:degraded` has no compile-time contract and no test covers the `main.ts` wiring.
- An empty (0-byte) `review-2026-07-29.md` sits untracked in the archived track directory.

## 4. Risks or blockers

1. **NFR-2 is probably violated by the FR-4 amendment, and was not re-checked.** The NFR's own workload (50 searches/day × 25 titles ≈ 37.5k titles/month) at the router's measured floor of $0.142/1000 is **≈$5.33/month — 2.7× the $2 budget**. The model NFR-2 was written against (`gemini-2.5-flash-lite`, $0.047/1000) came in at ≈$1.76. Escalation and upward router drift push it further: the top rung ($1.827/1000) would be ≈$68/month. Caveat: the $/1000 figures were measured on 8-title calls, so 25-title batching amortises prompt tokens and the real number is somewhat lower — but the gap is large enough that it needs measuring, not assuming.
2. **`parseFiles()` remains a silent-degradation path** — library scanning can fall to regex indefinitely with nothing but a `console.log` and no SSE event.
3. **`parser:degraded` has no consumer.** Zero references outside `main.ts` in server, `app/`, or the Flutter client. FR-9's "so the SSE/notification surface can show them" is published but not surfaced.
4. **Router non-determinism** undermines the benchmark as a regression gate — a future model/prompt change can't be distinguished from run-to-run routing variance at ±10 points on `title`.
5. Open from the track itself: the `OPENROUTER_API_KEY` exposed in a prior transcript is **still unrotated** (High, operator action).

## 5. Recommended next actions

1. **Rotate `OPENROUTER_API_KEY`** — highest-severity open item, operator action, unblocked.
2. **Re-measure cost at production batch size and reconcile NFR-2** — either the NFR's number moves with a recorded justification, or `RELEASE_PARSER_MIN_CODING_SCORE` / the default model changes. Requires surfacing `generateText`'s `usage` from `parseBatch()`, which also closes the FR-5 cost gap.
3. **Instrument `parseFiles()` with the FR-9 reporting** — `reportThrown` / `reportIfSlow` / `fell_back_to_regex`. Small, mechanical, closes the last silent path.
4. **Correct the record**: amend AC-3 and AC-4 in the archived spec (or note them superseded), and reconcile the 87.5 / 91.7 / 97.9 title figures to the artifact with a note that the router makes the number non-reproducible. Delete the empty `review-2026-07-29.md`.
5. **Consume `parser:degraded`** — a typed event union plus a UI/notification surface; otherwise the loud-failure work is only loud in the server log.
