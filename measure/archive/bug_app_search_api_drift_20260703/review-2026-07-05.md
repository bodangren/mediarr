# Track Review — bug_app_search_api_drift_20260703 — 2026-07-05

## 1. Summary of changes

Commits in the last 24 hours (2026-07-04 → 2026-07-05):

- `899b7e9f` — `docs(measure): diagnose app search API drift failures`
  - Added a detailed Phase 1 findings section to `plan.md`.
  - Documented the RED test run, exact failure messages, API signature inspection, and component wiring inspection.
- `9c4d9ee9` — `docs(measure): add Phase 1 SHAs and scope search-drift track to existing test files`
  - Marked Phase 1 tasks as complete with SHA evidence.
  - Marked Phase 4 “missing view test files” tasks as complete (verification that the files do not exist).
  - Updated `spec.md` acceptance criteria to remove `CalendarPage`, `MoviePosterView`, and `SeriesOverviewView` because those test files do not exist in the repo.

No source code or test files were changed in this period.

## 2. Spec alignment

| Acceptance Criterion | Status | Evidence |
|---|---|---|
| `SeriesInteractiveSearchModal` search-level tests pass | Partial | Phase 1 diagnosis confirms season/episode tests pass with longer timeout; pagination test still fails because component sends `pageSize: 500` while tests expect `pageSize: 100`. |
| `MovieInteractiveSearchModal` pagination test passes | Partial | Same `pageSize: 500` vs `100` mismatch documented; other timeout-related tests pass with longer timeout. |
| `PageLayout` More-menu navigation test passes | Partial | Passes with `--testTimeout=30000`; default 5 s timeout is insufficient for the first render. |
| `cd app && bun run test -- <affected-files>` passes | Missed | Not yet attempted; requires resolving the pagination mismatch. |
| Root `CI=true npm test` shows no new failures in these files | Missed | Not yet attempted. |

Overall alignment: **Partial**. The scope was correctly narrowed and the root cause (pageSize mismatch) was accurately identified, but the actual fix has not been implemented yet.

## 3. Code quality observations

- **Documentation quality is high**: the Phase 1 findings include exact line numbers, failure counts, and a clear contract-mismatch summary.
- **Scope reduction is well-justified**: removing acceptance criteria for non-existent test files (`CalendarPage.test.tsx`, `MoviePosterView.test.tsx`, `SeriesOverviewView.test.tsx`) keeps the track focused and avoids chasing phantom failures.
- **No source/test changes yet**, so there is no new code to review.
- The plan follows the TDD/RED-GREEN pattern: Phase 1 is complete, Phases 2–5 remain open.

## 4. Risks or blockers

- **Toolchain unavailable locally**: `node`, `npm`, and `bun` are not installed in this maintenance environment, so the reviewer could not independently verify the test run or pageSize fix. Verification depends on the existing CI/agent environment.
- **pageSize decision unresolved**: The plan notes Phase 2 must “Decide correct `pageSize` for interactive search (100 or 500)”. Changing the component from 500→100 may affect user experience (fewer releases fetched per round), while changing tests from 100→500 may mask a product decision. This needs an explicit product/engineering call.
- **25 unpushed commits** exist on this branch; pushing will surface all of them, including the work from this track.

## 5. Recommended next actions

1. **Resolve the `pageSize` mismatch** in Phase 2:
   - If `500` is the intended production behavior, update the two pagination tests to expect `pageSize: 500`.
   - If `100` is the intended contract, change `SEARCH_PAGE_SIZE` in both `SeriesInteractiveSearchModal.tsx` and `MovieInteractiveSearchModal.tsx` to `100`.
2. **Stabilize timeouts**: add `testTimeout` or per-suite `vi.setConfig({ testTimeout: … })` so the season/episode and `PageLayout` tests pass under the default runner.
3. **Run affected test files** (`SeriesInteractiveSearchModal.test.tsx`, `MovieInteractiveSearchModal.test.tsx`, `PageLayout.test.tsx`) and confirm green.
4. **Run root `CI=true npm test`** to ensure no regressions.
5. **Proceed to Phase 6 closeout**: update `measure/tech-debt.md` and archive the track once tests pass.
