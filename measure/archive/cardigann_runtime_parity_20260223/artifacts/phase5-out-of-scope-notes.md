# Phase 5 Out-of-Scope Notes

The following Cardigann behaviors remain explicitly out of scope for this track and are captured for follow-up parity work:

- Full template parity for all upstream control/function nodes beyond the currently required subset.
- Runtime evaluation of deeply nested template expressions inside arbitrary filter argument payloads.
- Complete parity for non-preset definitions not used by Mediarr's active imported indexer presets.
- End-to-end Cloudflare/challenge bypass behavior and headless-browser anti-bot execution flows.
- Full Cardigann login/session orchestration parity for every tracker-specific auth variant.
