# Daily Review — chore_untested_server_services_20260526 (2026-07-01)

## 1. Summary of changes

- The track was archived today:
  - Commit `f7623f84` moved the directory from `measure/tracks/` to `measure/archive/` and updated `measure/tracks.md`.
  - Commit `87968dc6` removed the now-empty track directory from `measure/tracks/`.
  - No source or test code changes were made in the last 24 hours; the implementation was completed in earlier commits.
- Original deliverable (as restructured on 2026-06-07): unit tests for the four runtime-critical server services — `Scheduler`, `EpisodeService`, `SeriesService`, and `MediaSearchService`.
- Actual outcome:
  - `Scheduler.test.ts` added (15 tests).
  - `SettingsService.test.ts` was also added (7 tests) despite being originally deferred.
  - `EpisodeService.ts` and `SeriesService.ts` were deleted (6-line aliases consolidated into `MediaService`); coverage moved to `MediaService.test.ts`.
  - `TvSearchService.ts` was deleted as an orphan alias; its legacy test migrated to use `MediaSearchService`.
  - `MediaSearchService` coverage was already provided by existing sibling test files (`searchAllIndexers`, `grabRelease`, etc.).
  - `SubtitleNamingService.test.ts` (18 tests) and `SubtitleRequirementEngine.test.ts` (21 tests) were completed despite being in the deferred bucket.
  - Remaining stories (S2 `SettingsService` is now done; S5, S9, S10) are deferred to post-v1.0.

## 2. Spec alignment

**Met for closeout; partial against the original 10-service spec.**

- The archival step matches the plan's verification/closeout phase.
- Runtime-critical services are covered.
- Deferred stories (`TvSearchService`, `SubtitleProviderFactory`, `FilterService`) are explicitly documented and moved to the post-v1.0 backlog.
- The 7 remaining pre-existing `MediaSearchService` timeout failures were documented in `tech-debt.md` and are now owned by the new track `bug_mediasearch_timeout_tests_20260630`.

## 3. Code quality observations

- Tests use the project's `vi.hoisted()` + `vi.mock()` pattern consistently.
- The plan contains extensive build-graph evidence and spec-vs-HEAD API mismatch notes (e.g., `TvSearchService` orphan alias, `SubtitleNamingService` `buildSubtitlePath` vs. spec's `generatePath`, `SubtitleRequirementEngine` `RequirementResult` shape). This is valuable context for the deferred follow-up track.
- A production change (`TorrentManager.ts` line 879: `Number(uploadedBaseline)`) and companion test updates were made while fixing pre-existing BigInt/Number failures. These were not originally in scope but were necessary to unblock the suite.
- The shared migration `drizzle/0003_workable_sage.sql` also contains unrelated `schedulerEnabled` column work from `feature_scheduler_automation_dashboard_20260524`.

## 4. Risks or blockers

- **Deferred coverage gaps:** `SubtitleProviderFactory` and `FilterService` still lack dedicated unit tests.
- **Shared migration:** `0003_workable_sage.sql` mixes columns from independent tracks, complicating rollback reasoning.
- **Pre-existing Bun/sqlite failures:** Several integration tests fail or must be skipped under Bun because `better-sqlite3` is not supported; this is an environment limitation, not a track defect.
- **No new regressions introduced today:** Only archival bookkeeping changed.

## 5. Recommended next actions

1. Create a post-v1.0 follow-up track for the deferred server-service stories (`TvSearchService` already deleted; `SubtitleProviderFactory`, `FilterService`, and any remaining low-risk services).
2. Continue the `bug_mediasearch_timeout_tests_20260630` work to eliminate the 7 pre-existing timeout failures documented by this track.
3. Monitor the shared `0003_workable_sage.sql` migration for conflicts with `feature_scheduler_automation_dashboard_20260524`.
4. Archive is complete; no further action required for this track.
