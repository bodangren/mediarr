# Track Review: indexer_health_monitoring_20260509
**Review date:** 2026-06-20  
**Review window:** commits since 2026-06-19  
**Reviewer:** Kimi Code CLI (subagent)

## Summary of Changes

This track saw heavy activity in the last 24 hours, moving from Phase 1 Red through Phase 3 Green and into Phase 4 verification.

**Commits (oldest → newest):**
- `12acd8bb` test(indexer-health): Phase 1 Red — failing tests for health service, repo extensions, route
- `2c116173` feat(indexer-health): implement health check service and route (Phase 1 Green)
- `269bce9d` test(indexer-health): Phase 2 Red — auto-disable orchestrator + live proof + search-skip regression
- `2db7448c` feat(indexer-health): implement auto-disable orchestrator and wire into IndexerTester (Phase 2 Green)
- `22072eb7` test(indexer-health): Phase 3 Red — UI badge integration, re-enable, tooltip, history assertions
- `ac22bf84` feat(indexer-health): implement Phase 3 UI — health badge, re-enable button, and tooltip integration
- `c11188b2` fix(indexer-health): Phase 3 re-enable uses existing PUT /api/indexers/:id
- `21600e75` docs(indexer-health): Phase 3 adversarial audit attempt-2 — pass with 2 non-blocking findings
- `c43f6916` / `7fe331a4` docs(indexer-health): Phase 4 verification — track surface GREEN, pre-existing gate failures documented

**Files changed in the review window:** 15 files, +913 insertions / −180 deletions.  
Key production files: `IndexerHealthRepository.ts`, `IndexerHealthService.ts`, `IndexerAutoDisable.ts`, `IndexerTester.ts`, `indexerRoutes.ts`, `IndexerHealthBadge.tsx`, `SettingsIndexersPage.tsx`, plus tests and Measure docs.

## Spec Alignment

**Overall: Partial / Mostly Met.**

All six spec acceptance criteria now have implementation and tests:

| Acceptance Criterion | Status | Evidence |
|---|---|---|
| Health check endpoint or background job tests each indexer periodically | Met | `GET /api/indexers/:id/health` route + `IndexerHealthService.ping()` |
| Health metrics stored per indexer (lastSuccessAt, failureCount, avgResponseTimeMs) | Met | `IndexerHealthRepository` extensions + schema |
| Auto-disable indexer after N consecutive failures (configurable threshold) | Met | `IndexerAutoDisable` + `IndexerTester` wiring |
| Manual re-enable UI in settings | Met | `IndexerHealthBadge` re-enable button + `SettingsIndexersPage` |
| Search queries skip disabled indexers automatically | Met | Regression test in `MediaSearchService.searchAllIndexers.test.ts` |
| Tests pass (backend + component tests), build/typecheck clean | Partial | Track-surface tests pass on correct Node version; Phase 4 “all-green” gate is blocked by pre-existing failures outside this track and a local better-sqlite3 ABI mismatch (see Risks). |

## Code Quality Observations

- **TDD discipline is strong.** Each phase lands Red tests before Green implementation, with explicit targeted Red commands and fail counts recorded in `plan.md`.
- **Architecture guardrails respected.** Auto-disable mutates the existing `indexers.enabled` column; manual re-enable reuses the existing `PATCH /api/indexers/:id` endpoint rather than inventing a new “undisable” route.
- **Concurrency handled.** `recordFailure` uses `onConflictDoUpdate()` for atomic increment under parallel calls.
- **UI is accessible.** Badge uses `data-testid`, `data-indexer-id`, and an `sr-only` description.
- **Review finding NB-1 is real but non-blocking:** `failureCount` is not reset to 0 on manual re-enable, so the next failure will immediately re-disable the indexer. The plan recommends a follow-up track `chore_reset_failure_count_on_reenable_20260620`.
- **Test noise:** React Testing Library emits `act()` warnings during `SettingsIndexersPage` tests (state updates after initial render). All tests pass, but cleanup would reduce warning noise.
- **Process noise:** Multiple supervisor-gate fix commits (stashes, reverts, re-stashes) show the Red-phase boundary check is fighting pre-existing dirty worktree items. The history is preserved, but the stash-heavy workflow is fragile.

## Risks or Blockers

1. **Environment blocker — better-sqlite3 ABI mismatch.**  
   Local run of the server-side track tests failed with:
   ```
   NODE_MODULE_VERSION 127 vs 137
   ```
   The environment is running Node v24.4.0; `better-sqlite3` appears compiled for Node v22 (MODULE_VERSION 127). The plan notes specify Node v22.22.3 via nvm, but nvm is not available in this shell. Until the tests run on the correct Node version, the Phase 1/2 live proofs cannot be locally verified.

2. **Phase 4 “all green” gate is structurally blocked.**  
   The broader app suite has 137 pre-existing failures and 4 pre-existing typecheck errors in files untouched by this track. The plan correctly classifies Phase 4 as `PARTIAL` and recommends a separate `chore_app_pre_existing_failures_20260620` track.

3. **failureCount not reset on re-enable** creates a user-visible edge case where a manually re-enabled indexer can be immediately auto-disabled again on the very next failure.

4. **Worktree stash debt.** Several stashes preserve unrelated items (CONDUCTOR archive timestamp, etc.). If not restored or dropped, they may confuse future sessions.

## Recommended Next Actions

1. **Run server-side track tests on Node v22.22.3** (the version noted in the plan) to confirm the Phase 1/2 live proofs still pass.
2. **Create the follow-up track** `chore_reset_failure_count_on_reenable_20260620` to reset `failureCount` to 0 when an indexer is manually re-enabled.
3. **Create or prioritize** `chore_app_pre_existing_failures_20260620` to clean up the 137 pre-existing app test failures / 4 typecheck errors so the indexer-health Phase 4 gate can be closed.
4. **Restore or drop** the stashed unrelated items before the next session to avoid repeated supervisor-gate conflicts.
5. **Archive this track** once the environment verification and follow-up tracks are scheduled; the track surface itself is functionally complete.
