# Track Review: Settings and TV Search Service Test Coverage

**Review date:** 2026-07-10
**Reviewer:** Measure daily automation
**Commit reviewed:** `c79266ee` — chore(measure): add daily review reports 2026-07-09 for split test coverage tracks

---

## Summary of changes

No new code changes for this track in the last 24 hours. The only commit (`c79266ee`) added the previous daily review report (`review-2026-07-09.md`).

`SettingsService.test.ts` already exists, and `TvSearchService` still does not exist as a service class in `server/src/services/` (the orphan alias was deleted in the archived `chore_test_infrastructure_hardening_20260612` track). The track remains unexecutable as written.

---

## Spec alignment

**Verdict: missed**

- **SettingsService:** The spec still claims it has no dedicated tests, but `server/src/services/SettingsService.test.ts` exists with 8 tests covering `get`, `update`, `replace`, and error propagation.
- **TvSearchService:** The spec still treats `TvSearchService` as a real service. A repository search confirms no `class TvSearchService` exists in `server/src/`. TV search orchestration is handled by `WantedSearchService` and `MediaSearchService`.
- Because one of the two target services does not exist, the `TvSearchService` acceptance criteria cannot be met without redefining the target.
- The `SettingsService` acceptance criteria describe repository-level concerns (defaults, validation, serialization) rather than the thin service-wrapper surface.

---

## Code quality observations

- No new code to review.
- The existing `SettingsService.test.ts` follows the project’s repository-mock pattern.
- The plan’s Phase 2–5 TDD cycle for `TvSearchService` is still unexecutable.

---

## Risks or blockers

| Risk | Impact | Likelihood |
|------|--------|------------|
| `TvSearchService` does not exist — track is unexecutable as written | Critical | Certain |
| SettingsService tests already exist; plan would duplicate effort | Medium | High |
| Acceptance criteria describe repository concerns, not service concerns | Medium | High |
| Track may create confusion by resurrecting a deleted alias name | High | Medium |

---

## Recommended next actions

1. Halt execution of this track as written.
2. For `SettingsService`: run `SettingsService.test.ts` with coverage. If ≥80%, close the SettingsService portion with a documentation update.
3. For TV search: decide whether the intent is to test `WantedSearchService`, `MediaSearchService`, or both. Review existing tests for those services and identify TV-specific gaps.
4. Create a new, correctly named track (e.g., `wanted_search_tv_coverage_YYYYMMDD` or `media_search_tv_coverage_YYYYMMDD`) tied to the actual service APIs.
5. Update `measure/tech-debt.md` and `measure/tracks.md` to remove or correct the `TvSearchService` reference.
6. Archive or rename this track rather than keeping it as a pending item in its current form.
