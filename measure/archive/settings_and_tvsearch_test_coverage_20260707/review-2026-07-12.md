# Track Review: Settings and TV Search Service Test Coverage

**Review date:** 2026-07-12
**Reviewer:** Measure daily automation
**Commits reviewed:** `f863f614` — docs(measure): add daily review reports 2026-07-11 for changed tracks

---

## Summary of changes

No new implementation work in the last 24 hours. `f863f614` only added the previous daily review report (`review-2026-07-11.md`). No source files, tests, specs, or plans were modified.

`SettingsService.test.ts` exists in `server/src/services/` with 7 passing tests. `TvSearchService` still does not exist as a service class in `server/src/services/` (the orphan alias was removed in the archived `chore_test_infrastructure_hardening_20260612` track).

## Spec alignment

**Verdict: partial**

- **SettingsService — met:** `SettingsService.test.ts` passes and achieves **100% branch coverage** on `SettingsService.ts`.
- **TvSearchService — missed:** The spec still treats `TvSearchService` as a real service. No `class TvSearchService` exists in `server/src/`; TV search orchestration is handled by `WantedSearchService` and `MediaSearchService`.
- Because one of the two target services does not exist, the `TvSearchService` acceptance criteria cannot be met without redefining the target.

## Code quality observations

- No new code to review.
- Existing `SettingsService.test.ts` follows the project’s repository-mock pattern.
- The plan’s Phase 2–5 TDD cycle for `TvSearchService` is unexecutable.

## Risks or blockers

| Risk | Impact | Likelihood |
|---|---|---|
| `TvSearchService` does not exist — track is unexecutable as written | Critical | Certain |
| Track has been stalled for multiple days without corrective action | Medium | Certain |
| Acceptance criteria describe repository concerns, not service concerns | Medium | High |
| Track may create confusion by resurrecting a deleted alias name | High | Medium |

## Recommended next actions

1. Halt execution of this track as written.
2. Close the `SettingsService` portion immediately (100% branch coverage, 7/7 tests passing) and update `measure/tech-debt.md` to remove the SettingsService coverage gap.
3. For TV search, decide whether the intent is to test `WantedSearchService`, `MediaSearchService`, or both. Review existing tests for those services and identify TV-specific gaps.
4. Create a new, correctly named track (e.g., `wanted_search_tv_coverage_YYYYMMDD` or `media_search_tv_coverage_YYYYMMDD`) tied to the actual service APIs.
5. Archive or rename the current track to reflect that the SettingsService portion is complete and the TvSearchService target is obsolete.
