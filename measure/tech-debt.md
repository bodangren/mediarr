## Tech Debt Registry

> This file is curated working memory, not an append-only log. Keep it at or below **50 lines**.
> Remove or summarize resolved items when they no longer need to influence near-term planning.
>
> **Severity:** `Critical` | `High` | `Medium` | `Low`
> **Status:** `Open` | `Resolved`
>
> Pruned 2026-07-26: 30 long-settled `Resolved` rows removed (drizzle migration residue, Prisma
> naming, MSW coverage, lint debt, catalog caching, scheduler persistence, variant-subtitle and
> import-list coverage, primitives/modal/table test-quality items). Their closure evidence lives
> in the archived tracks that closed them. Only rows that still steer near-term planning remain.

| Date | Track | Item | Severity | Status | Notes |
|------|-------|------|----------|--------|-------|
| 2026-03-30 | chore_drizzle_migration_20260314 | DB recreated from scratch — existing data lost | Medium | Resolved | Closed 2026-07-27 by `database_migration_strategy_20260713`. The mechanism was found, not just mitigated: drizzle-orm applies all pending migrations in ONE transaction, SQLite treats `PRAGMA foreign_keys` as a no-op inside a transaction, so `DROP TABLE Series` in the `0001` rebuild cascade-deleted every `Season`/`Episode` row **while committing successfully**. `server/src/db/migrationRunner.ts` sets the pragma outside the transaction, gives each migration its own transaction, and runs `PRAGMA foreign_key_check`. Regression: `migrationDataPreservation.test.ts` |
| 2026-07-27 | database_migration_strategy_20260713 | No reverse (down) migrations | Low | Open | Drizzle generates no down-migrations, so rolling back an **applied** migration means restore-from-backup (documented in `docs/migration-runbook.md`). Rollback of a **failed** migration is handled transactionally. Hand-authoring inverses for every migration is a separate track with no test oracle proving the inverse is faithful — deliberately not attempted |
| 2026-07-27 | database_migration_strategy_20260713 | `scripts/` → `server/` static imports silently break at runtime | Medium | Open | Root tsconfig is `module: nodenext`; `server/tsconfig.json` is `module: preserve`. A static named import across that boundary collapses to a default-only CJS namespace and fails at load with *"does not provide an export named …"*. This had silently broken `scripts/reconcile-migration-compatibility.ts` — so the documented `npm run migrate` could not run on Node 22. Both scripts now use dynamic `import()`. **Any new file in `scripts/` importing from `server/` must do the same**, or unify the two module settings |
| 2026-07-28 | chore_remaining_server_service_coverage_20260728 | `MetadataProvider` computes `tmdbCollectionId` on search, then drops it | Low | Open | `searchMovies` sets `tmdbCollectionId: movie.belongs_to_collection?.id ?? undefined`, but all three `searchMedia` mappings (unified, TV-only, movie-only) rebuild the result field by field and omit it. `searchMovies` is private and called only from `searchMedia`, so the computation is dead and collection membership never reaches a search-result consumer — though `getMovieDetails` does surface it on the details path. Pinned by a characterisation test; whether search results *should* carry collections is a product decision, so not "fixed" by widening the mapping. Related: `SeriesSearchResult` does not declare `popularity` even though `searchSeries` always sets it, which is why `searchMedia` reads it back through `(result as any)` — the interface is out of step with its mapper |
| 2026-07-28 | chore_remaining_server_service_coverage_20260728 | `LibraryScanner` aborts an entire scan on one unreadable subdirectory | Medium | Open | **Confirmed by test, and live** — `LibraryScanner` is wired in production. `getAllFiles` recurses with no per-directory error handling and the `fs.access` guard covers only the scan root, so a single `EACCES` anywhere below it rejects the whole `scanSeries`/`scanMovie` call. Worse, the walk collects every path *before* parsing any, so an abort mid-walk links **nothing** — not even files already enumerated from readable directories. A user migrating a messy library hits this on one bad permission bit and sees a silent no-op. Pinned by characterisation tests in `LibraryScanner.test.ts` (`known defect:` block, skipped when running as root) so the fix is deliberate. Fix: `try/catch` per directory, skip and warn. Unbounded recursion on symlink loops is a second, unpinned hazard in the same function |
| 2026-07-28 | chore_remaining_server_service_coverage_20260728 | Local metadata-sidecar generation is unimplemented, not just unwired | Low | Open | `MetadataGenerator` was deleted this track as dead code (no production callers; artwork is served from remote TMDB URLs via `posterUrl`, never from disk). Its replacement-in-waiting, `Organizer.colocateMovieMetadata`, is **also never called in production** — so Mediarr writes no `.nfo`/artwork sidecars at all, and libraries are not readable by Kodi/Jellyfin/Plex scrapers. Kept because it is the better foundation: generic (`filename`, `content`), already tested. **If this feature is ever built, do not reintroduce the two defects the deleted file had** — `escapeXml` called unguarded on possibly-undefined fields, and `Buffer.from(body, 'binary')` on a string that `HttpClient` already UTF-8-decoded (`HttpClient.ts:98`), which silently corrupts every image. Binary download needs an `arrayBuffer()` path on `HttpClient` |
| 2026-07-28 | chore_home_network_deployment_hardening_20260712 | Container build layers get a 1024-fd soft limit — build tooling must stay under it | Low | Resolved | Root cause of the two-week intermittent `Rollup failed to resolve import` clean-image defect. A buildah `RUN` layer allows **1024** fds (verified: `podman run` and the host both give 1048576 — different namespaces), while rollup defaults `maxParallelFileOps` to **1000**; measured peak demand was **1029**, five over, which is why it was intermittent. Fixed by `maxParallelFileOps: 128` in `app/vite.config.ts` (peak 156), guarded by `tests/spa-build-file-parallelism.test.js`. **Forward-looking constraint: any future build-time tool added to the image — test sharding, parallel codegen, a bundler swap — must keep concurrent open files well under 1024.** Reproduce cheaply on the host with `ulimit -n 1024`, do not reach for a 7-13 min image build |
| 2026-03-30 | chore_drizzle_migration_20260314 | `server/src/db/index.ts` uses `bun:sqlite` — no type declarations in tsx/tsc context | Low | Open | Works at runtime under Bun; add `declare module 'bun:sqlite'` shim if strict tsc is needed before a full Bun switch |
| 2026-04-13 | review_20260413 | Autonomous checkpoints bundle too many files per commit (4c78a41: 121 files / +6409 lines) | Medium | Open | Process guidance: split autonomous checkpoints per subsystem so review granularity is preserved |
| 2026-07-26 | filter_service_test_coverage_20260707 | Server-service coverage gap (originally "7 of 55 lack a sibling `.test.ts`") | Medium | Amended | **Closed as written, but not as a clean Resolved — the row measured the wrong thing.** "Has a sibling `.test.ts`" is a filename check, not a coverage claim. Outcome of `chore_remaining_server_service_coverage_20260728`: `ProbeMetadataParser` 75.51%→**100%** branch, `LibraryScanner` 71.42%→**100%**, `MetadataProvider` 60.22%→**94.31%** (residual branches unreachable), `ActivityEventEmitter` 0%→**100%**. Three caveats that forbid a plain Resolved: (a) `MetadataGenerator` was **deleted** as dead code, so the count fell without anything being covered; (b) `DataDirectoryInitializer` was **already at 90%** branch via a non-sibling file and `WantedService` reports **100% only because it has zero branches** — neither was a real gap; (c) both had been well exercised by non-sibling tests all along. **Any future coverage row must state a measured threshold, not a file-naming convention.** Aggregate for the six surviving services: 100% stmt/func/line, 97.71% branch |
| 2026-07-28 | bug_ai_release_parser_lockdown_20260728 | `OPENROUTER_API_KEY` appeared in a conversation transcript | Low | Resolved | Closed 2026-07-28 as **accepted risk, by operator decision**: the transcripts are not public, so the exposure has no audience and rotation is not warranted. The key stays live at `.env` (mode 600, gitignored via `.gitignore:27`). Reopen only if transcript handling changes |
| 2026-07-28 | bug_ai_release_parser_lockdown_20260728 | `regexFallback` misclassifies a season range as a season pack | Medium | Open | `\bS\d{1,2}\b` matches the leading `S01` of `S01-S09`, so a complete-series release is reported as `season_pack` season 1 instead of `null`. Not cosmetic: `WantedSearchService.ts:408` skips `season_pack` when hunting complete series, so the defect can hide a legitimate release from an automatic grab. Pinned by a characterisation test in `ReleaseParser.golden.test.ts`; fixing it is a deliberate change, not a fixture edit. Same function also destroys internal punctuation (`House.M.D.` → `House M D`) |
| 2026-07-28 | bug_ai_release_parser_lockdown_20260728 | AI parse **accuracy** measured; `title` is the weakest field | Low | Open | Benchmarked over the 48-entry golden set at the production batch size of 25 via `npm run benchmark:parser`. `openrouter/pareto-code` at `min_coding_score=0`: 48/48 aligned; `year`, `seasonNumber`, `episodeNumbers`, `matchType`, `type`, `quality.resolution` all **100%**; `title` **91.7%** (44/48). The 4 title misses are unclassified — they may be fixture disagreements rather than model errors, and nothing in production reads `parsedRelease.title` from the batch path (`CustomFormatScoringEngine.ts:94` reads only `relevanceScore`), so this is low priority. Re-run the benchmark before changing model or prompt |
| 2026-07-28 | bug_ai_release_parser_lockdown_20260728 | Router cost and latency are variable by construction | Medium | Open | `openrouter/pareto-code` selects by *coding* percentile, which is the wrong axis for filename parsing, and its shortlist will drift toward expensive frontier models. Measured $0.142/1000 titles at `min_coding_score=0` (routed to `gpt-5.6-luna`) vs $0.047 for a pinned `gemini-2.5-flash-lite` — ~3× — and the dial spans 12.9× to $1.827 at score 0.75. Latency also varies run to run: the same 25-title batch measured 13.3s and 28.6s. Mitigated, not solved, by the 60s deadline, the ≥60% slow-call warning, and `parser:degraded` SSE events. **Re-run `npm run benchmark:parser` periodically**; if cost drifts, pin a fast model and set `OPENROUTER_MODEL` |
| 2026-07-26 | settings_and_tvsearch_test_coverage_20260707 | `SettingsService` coverage target was vacuous — the real branch surface is `AppSettingsRepository` | Low | Open | `SettingsService.ts` is a 3-method pass-through to the repository with **zero branches**, so its "≥80% branch coverage" criterion was unfalsifiable. `AppSettingsRepository.ts` is 582 LOC with ~34 branch points and has its own test file; measure that file's real branch coverage in a follow-up before claiming settings are covered |
| 2026-07-26 | measure_process | Track specs describe services and behaviour that do not exist in the codebase | Medium | Open | Two instances found in one day: `filter_service_test_coverage` specified quality/language/size/custom-format filters (FilterService actually filters series/indexer **conditions**), and `settings_and_tvsearch_test_coverage` specified `TvSearchService`, deleted 6 weeks earlier. Specs are being written from assumption rather than from source. Add a source-verification step to `new-track` before acceptance criteria are fixed |
| 2026-07-26 | measure_process | Track registry drifts from reality; completed work stays marked pending | Medium | Open | 7 app tracks (~106 "open" tasks) were fully satisfied by upstream work yet still listed as pending/in-progress, and 3 coverage tracks were specced as greenfield when the test files already existed. Reconcile `tracks.md` against actual test/typecheck state at the start of every session, not just at archive time |
| 2026-04-09 | review_20260409 | `as any` casts persist across the app workspace (29 occurrences in `app/src`) | Low | Open | The specific `SeriesDetailPage` case is closed (see below), but the general pattern remains. Not release-blocking; typecheck and build are clean |
| 2026-07-26 | chore_server_strict_typecheck_20260713 | Server strict typecheck as a release gate | Low | Resolved | `npx tsc -p server/tsconfig.json --noEmit` exits 0 with zero diagnostics, re-verified after all 2026-07-26 test additions. Strict typecheck is now a real gate rather than a known exception |
| 2026-07-26 | filter_service_test_coverage_20260707 | `FilterService` had 68.48% branch coverage | Medium | Resolved | Raised to **100%** branch/stmt/func/line; 26 → 64 tests. No production bugs found; `FilterService.ts` unmodified |
| 2026-07-26 | subtitle_services_test_coverage_20260707 | Subtitle services below full branch coverage | Medium | Resolved | All three at **100%** branch coverage, 51 → 56 tests. `SubtitleRequirementEngine` 89.13% → 100% (`isCutoffMet` audio include/exclude paths, empty-string language codes); `SubtitleProviderFactory` 90.9% → 100% (`getProviderUnavailableReason` null fallback); `SubtitleNamingService` was already 100%. No source changes |
| 2026-06-13 | chore_test_infrastructure_hardening_20260612 | `TvSearchService` no longer exists | Low | Resolved | Deleted as an orphan alias (commit `037418f`). Season/episode dispatch, aggregation, and indexer error handling now live in `MediaSearchService.ts` (`searchEpisode`, `searchAllIndexers`, `searchWithTimeout`), covered by three sibling test files. Spec amended rather than silently dropped |
| 2026-04-09 | review_20260409 | `SeriesDetailPage` mapped API responses via `as any` casts | Low | Resolved | Verified 2026-07-26: zero `as any` remain in `app/src/pages/SeriesDetailPage.tsx`. Closed by archived track `type_series_api_response_20260508`; the row had been stale for weeks |
| 2026-07-03 | chore_app_pre_existing_failures_20260630 | Historical app workspace failure inventory (133 failures / 34 files, later 75 / 21) | Medium | Resolved | `CI=true npm test --workspace=app`: **204/204 files, 1960/1960 tests pass**, 0 skipped; `npm run build --workspace=app` exits 0. All 7 focused split tracks reconciled and archived 2026-07-26 |
| 2026-03-14 | chore_shadcn_setup_20260314 | `FileBrowser.test.tsx` uses static fixtures instead of parent-driven path updates | Low | Resolved | Accepted by design: the component receives entries as props, so static fixtures are the correct unit-test shape for callback behaviour. Closed as won't-fix rather than left perpetually Open |
