# Review: Home Network Deployment Hardening — 24h window ending 2026-07-15

**Scope reviewed:** 5 commits (`a2950ff5` → `49f29a2f`), 138 files, +7,948/−1,046, compared against the track's `spec.md` (FR-1…FR-8) and `plan.md` (Phases 1–6).

## 1. Summary of Changes

- **`a2950ff5`** (07-14 05:06) — the bulk landing. Although its message says "archive completed Scheduler & Automation Dashboard track," it contains essentially all of the track's Phase 1–6 implementation: the `Dockerfile` (fail-closed CMD chain: preflight → migration-ledger reconcile → `drizzle-kit migrate` → daemon, joined with `&&`), `docker-compose.yml` (required `PUID:PGID`, host networking, bind mounts, JSON-validating healthcheck), `.dockerignore`, drizzle `0003`/`0004` journal repair + new migration, README operational docs, and `.env.example`. It also bundles `.opencode/` session state and two compiled `.pyc` binaries.
- **`a3656ec6`** (07-14 21:31) — a 731-line Phase 6 test strategy narrowing remaining scope to one slice: clean-image workspace dependency resolution.
- **`13ea7272`** (07-14 22:33) — heavy Red contract `tests/clean-workspace-build.test.js` (live `docker build --no-cache`); proved non-deterministic as a Red gate.
- **`e4883b1b`** (07-14 23:22) — corrected deterministic Red contract `tests/clean-workspace-invariant.test.js`: a structural check that the Dockerfile or `app/vite.config.ts` uses one of four hoisting-independent install patterns. The heavy test is retained as the Green/acceptance gate.
- **`49f29a2f`** (07-15 05:03) — timestamp-only conductor artifact churn (acknowledged `npm test` side effect; one such modification sits in the working tree now).

I verified the claimed state live: running the four focused contract suites gives **26 passing tests and exactly 1 intentional failure** — the invariant test fails with the documented "workspace-install/build invariant violated" message. Plan evidence and reality match.

## 2. Spec Alignment

| Requirement | Status | Evidence |
|---|---|---|
| FR-1 Docker Engine compose, UID/GID, host net, no Podman-only options | **Met** (runtime proof human-gated) | `${PUID:?}:${PGID:?}`, `network_mode: host`, required bind mounts; no `keep-id`/`:Z` |
| FR-2 Fail-closed encryption key / config storage, no ephemeral DB | **Met** | `startup.test.ts` 9/9; preflight runs first in container CMD |
| FR-3 Versioned migrations that fail the container | **Met** | `&&`-chained `drizzle-kit migrate`; journal repaired; ledger reconciliation for legacy `push` DBs |
| FR-4 Secrets out of build context + healthcheck | **Met** | `.dockerignore` excludes `.env*`, `config/`, `data/`, `*.db`; healthcheck validates `body.ok === true` |
| FR-5 Ops documentation | **Met** | README: `sqlite3 .backup`, `integrity_check`, restore rehearsal, upgrade/rollback, trusted-LAN caveat |
| FR-6 Fail startup on unwritable `/data` | **Met** | `DataDirectoryInitializer` write/delete probes + default-path resolver; 10/10 green |
| FR-7 No fictional DB-only torrent engine | **Met** (per plan evidence) | Fallback deleted; torrent seam propagates init failure |
| FR-8 Migration rehearsal parity + safe legacy adoption | **Met** | README/`.env.example` use `file:$CONFIG_DIR/mediarr.db`; drifted-schema fixture rejected |
| Clean-image build reproducibility (image-build AC) | **Missed — deliberately open** | Fresh `--no-cache` builds fail non-deterministically (Rollup resolve family); Red contract landed, fix explicitly unowned |

**Overall: partial-complete.** All eight FRs are implemented and test-backed, but the track cannot close: Phase 6 has two `[~]` tasks — the image-build architectural fix and the human-gated Docker Engine / physical-device smoke checks.

## 3. Code Quality Observations

- **Strong:** the two-tier TDD repair (deterministic structural invariant as Red, heavy live build as acceptance Green) is a correct response to a flaky Red gate; family-level regexes avoid stale assertions, and the failure message is genuinely actionable.
- **Strong:** the fail-closed container CMD chain and compose `:?` required-variable syntax deliver exactly what the spec demanded.
- **High (hygiene):** `a2950ff5`'s commit message misdescribes ~90% of its content — the entire hardening implementation rode in under a scheduler-archive message, along with `.opencode/goals/` state and two `.pyc` binaries. This breaks the commit-per-task traceability the Measure workflow mandates (the plan notes commits were deferred "per user request," which explains but doesn't fix the audit trail).
- **Medium:** `clean-workspace-build.test.js` runs `docker build --no-cache` inside the plain vitest aggregate — slow, disk-hungry, and spuriously green on Podman-cached hosts (the very flakiness that forced the second Red). It should be env-gated.
- **Low:** the Dockerfile's "install after COPY" workaround for Podman COPY behavior deserves re-examination once a real Docker Engine host is available.

## 4. Risks / Blockers

1. **Blocker:** clean-image dependency resolution is still broken — a fresh Docker Engine deploy can fail at image build with `Rollup failed to resolve import`. This is the track's core deliverable and is intentionally unowned ("next role's work").
2. **Blocker (human-gated):** nothing has been verified on real Docker Engine or a physical Android/LAN device; all runtime smoke evidence is rootless-Podman.
3. **Risk:** host disk at 95% full (14G free, recorded in-plan) — heavy no-cache builds may fail environmentally and muddy Red/Green signals.
4. **Risk:** committed `.pyc`/`.opencode` artifacts plus the timestamp-churning conductor artifact create ongoing dirty-tree noise.

## 5. Recommended Next Actions

1. **Implement the Green:** add `RUN npm ci --workspace=app` after the root install (or `COPY --from=builder /app/app/node_modules ./app/node_modules` in the runner stage), confirm the invariant test passes, then run the heavy no-cache acceptance build.
2. **Env-gate the heavy build test** (e.g. skip unless `RUN_HEAVY_BUILD=1`) so the default aggregate stays fast and deterministic.
3. **Run the human-gated checks** on a real Docker Engine host — compose up, UID/GID write proof, `/api/health`, restart persistence — and record evidence in `plan.md`.
4. **Repo hygiene:** remove or gitignore the `.pyc` binaries and `.opencode/goals/` state; stop or ignore the conductor artifact timestamp churn; log both in `tech-debt.md`.
5. On completion, flip the two `[~]` Phase 6 tasks to `[x]`, archive the track, and update `tracks.md` per the workflow.
