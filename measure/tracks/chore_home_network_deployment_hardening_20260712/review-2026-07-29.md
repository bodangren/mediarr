I read the spec, plan, and all four track commits in the window, and re-ran the focused gates myself.

## Review: `chore_home_network_deployment_hardening_20260712` (2026-07-29)

### 1. Summary of changes

Four of the 29 commits in the last 24 hours belong to this track (the rest are `bug_ai_release_parser_lockdown_20260728` and `chore_remaining_server_service_coverage_20260728`). All four target Phase 6's single open blocker — the intermittent clean-image failure `Rollup failed to resolve import "<dep>" from "/app/app/..."`.

| Commit | Change |
|---|---|
| `d33499c1` | `scripts/docker-build-spa.sh` replaces the bare SPA build `RUN`; captures a resolver trace on failure and exits with the **original** status. Heavy no-cache build test gated behind `CLEAN_IMAGE_BUILD_TESTS=true` (`npm run test:clean-image`). `clean-workspace-invariant.test.js` stops pinning the Dockerfile's exact command text and asserts the structural invariant instead. |
| `1c8bbabb` | Wrapper also prints the build layer's own limits. That fired on first live failure and overturned the recorded exclusion: `ulimit -n` is **1024** in a buildah `RUN` layer, not the 1048576 measured under `podman run`/host. |
| `730a09ae` | The fix: `build.rollupOptions.maxParallelFileOps: 128` in `app/vite.config.ts`, guarded by `tests/spa-build-file-parallelism.test.js`. Rollup defaults to 1000; measured peak demand 1029 vs. a 1024 ceiling — a five-descriptor overshoot. Reproduced deterministically on the host in 40s under `ulimit -n 1024` with `EMFILE`. |
| `f118d36e` | Plan/registry bookkeeping only. |

Net source footprint is small: one Dockerfile line, one vite config block, one shell script, two test files, plus Measure docs.

### 2. Spec alignment

- **Met (earlier phases, unchanged here):** FR-1–FR-8 implementations are all in place; `tests/deployment-hardening.test.js` (7 tests) and `clean-workspace-invariant.test.js` (5) pass. I confirmed the compose healthcheck asserts `ok === true` plus a string `data.status`, matching FR-4/AC-6.
- **Met, but off-spec:** the last-24h work has **no FR or acceptance criterion covering it**. Reproducible image builds are a precondition for AC "image build is green," never stated as a requirement. The two-week blocker lived entirely in plan evidence. Spec should have grown an FR when the defect became the track's critical path.
- **Partial:** plan task 37 stays `[~]` — Docker Engine and physical Android/LAN playback verification are human-gated because this host's `docker` is Podman 4.9.3. Track is correctly `in_progress`, 19/20.
- **Missed (bookkeeping):** every one of the ten acceptance criteria in `spec.md` is still an unchecked `[ ]`, despite the plan asserting evidence for nine of them. The spec is the acceptance artifact; it currently reads as if nothing was accepted.

### 3. Code quality

Strong overall, and unusually disciplined about its own epistemics — the plan records a passing control run that would have re-excluded the true cause, rather than discarding it, and the fix rests on the demand measurement rather than build outcomes.

- **Good:** the guard asserts a *bound with headroom* (`≤ 1024/4`) rather than pinning 128, explicitly avoiding the anti-pattern from `53e27adf` that locked the broken Dockerfile in as a passing gate. The wrapper's exit-with-original-status property is the right call for an intermittent defect and was arm-tested.
- **Scope leak, worth attention:** `730a09ae` also flipped the `OPENROUTER_API_KEY` tech-debt row from **High/Open** to **Low/Resolved — "accepted risk, by operator decision."** That is a security-posture change belonging to a different track, buried in a Docker build commit. The decision may be sound; the traceability is not.
- **Three numbers for one margin:** the vite comment says "8x headroom," the test encodes a 4x factor, the plan reports 6.6x (156 vs 1024). All defensible individually, confusing together.
- **Guard scope:** the test asserts the value in `app/vite.config.ts`, not the cap actually in effect during the image build. A future `--config`/mode variant would bypass it silently. Low severity.
- **Wrapper cost post-fix:** on failure the script now re-runs the whole build under `DEBUG='vite:*'` — 7–13 minutes doubled. Fine while the mechanism was unknown; worth a decision now that it is known.

### 4. Risks / blockers

1. **No automated enforcement of the actual fix.** There is no `.github/workflows` in this repo, and the no-cache image build is now opt-in. The only thing running by default is a static config assertion. If someone raises the cap or the build's baseline fd usage grows, nothing catches it until a hand-run `npm run test:clean-image`.
2. **Human-gated verification (blocker b).** Docker Engine compose/build and physical Android/LAN playback cannot be closed on this host. The track cannot be honestly archived until an operator runs them.
3. **Confirmation is thin by design.** 3/3 green host builds at `ulimit -n 1024` and one clean-image build is weak evidence for a defect with a historical base rate that swung 2/16 → 3/4. The plan says as much. The peak-demand measurement carries the claim; treat build outcomes as confirmatory only.
4. **Forward constraint is real.** Any future build-time tool added to the image (test sharding, parallel codegen, bundler swap) must stay under 1024 concurrent fds. Recorded in `tech-debt.md`, but nothing enforces it.

### 5. Recommended next actions

1. **Tick the spec's acceptance criteria** (or annotate the two that are human-gated), so `spec.md` reflects the nine met criteria rather than reading as untouched.
2. **Hand the operator a checklist** for the Docker Engine + physical device gate — the exact `docker compose config`, `docker build`, mount/identity smoke, and LAN playback commands — since that is the only thing between this track and archival.
3. **Separate the `OPENROUTER_API_KEY` status change** into its own commit or note on the parser track, with the operator decision recorded where a reader would look for it.
4. **Decide the wrapper's fate now that the mechanism is known** — keep it as a cheap tripwire, or reduce the failure path to the environment block and drop the full `DEBUG='vite:*'` re-run.
5. **Reconcile the headroom prose** to one number, and consider asserting the cap against the resolved build config rather than the source file.
6. If CI ever lands, put `npm run test:clean-image` on a nightly rather than per-PR — it is a ~12-minute job.

I did not write this to `review-2026-07-29.md` (the empty file in your working tree) — say the word and I'll drop it there.
