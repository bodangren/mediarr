# Track Review: Browser-Verified Web Application

**Date:** 2026-07-30  
**Track:** `feature_browser_verified_web_app_20260729`  
**Window:** Last 24 hours

## Summary of Changes

Over the last 24 hours there were **11 commits** touching this track. Eight are code/test changes; three are plan-only evidence updates.

**Code changes outside `measure/`:**

- **Browser acceptance harness** (`50347226`): added `tests/browser/harness/disposableMediarr.ts` (≈743 lines), `browserFailures.ts`, `playwright.config.ts`, and `disposable-environment.pw.ts` to boot a real Mediarr daemon against isolated SQLite/config/media/backup roots with deterministic seed fixtures.
- **Notifications settings** (`322f88a2`, `b69c1336`): restored Fastify CRUD/test routes, Slack/Pushover transports, registry wiring, and the React settings page with an accessible delete-confirmation modal. Added focused app and server tests.
- **Route data/layout fixes** (`eb692f00`): repaired `RouteScaffold`, `PageLayout`, movie/series library and detail pages, plus server collection/movie route contracts and focused tests.
- **Static serving** (`6c6db2ae`): added SPA manifest revalidation with tests.
- **Poster fallback** (`c9eaf631`): shipped shared placeholder image and wired it into the disposable browser harness.

**Plan/spec updates:**

- Recorded Kimi WebBridge evidence, required Kimi acceptance over Browser Harness, and documented phase completions for route matrix, durable workflows, acquisition/recovery, and quality gates.

## Spec Alignment

| Requirement | Status | Evidence |
|-------------|--------|----------|
| FR-1 — Disposable browser acceptance environment | **Met** | `tests/browser/harness/disposableMediarr.ts` launches `tsx server/src/main.ts` with temp roots and seeds movies/series/episodes/variants/subtitles/activity/queue/collections/scheduler/settings. |
| FR-2 — Route and shell acceptance | **Met (core subset)** | Plan reports 64 cases across 32 `App.tsx` paths at desktop + 390×844 mobile, asserting landmarks, reload, no overflow, and no console/internal-request failures. |
| FR-3 — Durable user workflows | **Partial** | Notifications, scheduler toggle/interval/run-now, download-client settings, media-root settings, system events clear, backup create/list/delete/download/restore, movie-subtitle delete, and setup wizard are proven. Remaining: more settings CRUD, subtitles search/provider flows, full collections workflows. |
| FR-4 — Acquisition and recovery | **Partial** | Search-to-wanted, grab-to-queue, completion/import, provider failure/recovery, daemon restart recovery, SSE reconnect recovery, and manual subtitle download are proven. Remaining: restart/SSE recovery is closed; broader restart/reconnect matrix still needed per plan. |
| FR-5 — Browser performance and usability | **Partial** | `experience-quality.pw.ts` uses real navigation timing, axe `main` scan, console/request capture, and `?`/Escape keyboard checks. Dashboard widget headings fixed from H3→H2. Performance samples are diagnostic only, not a budget/p95 gate. |

**Acceptance criteria:**

- [x] Covered routes are no longer accepted solely from jsdom/mocked-client tests.
- [x] Tested read/write flows (settings, scheduler, backup, events, subtitles, download client, media root) are durable after hard reload.
- [~] Production browser suite runs from documented commands; multiple scenario files exist but a single top-level command is not explicitly verified.
- [x] Failure artifacts include console/network errors and screenshots/traces via `browserFailures.ts` and Playwright config.
- [~] Performance assertions are browser-derived for the quality suite, but no explicit thresholds/budgets are committed yet.

## Code Quality Observations

- **Focused test coverage is strong.** Most production changes are accompanied by route/contract/component tests (e.g., `notificationRoutes.test.ts`, `SettingsNotificationsPage.test.tsx`, `collectionRoutes.create.test.ts`, `movieRoutes.missing.test.ts`).
- **Accessibility improvements are consistent.** Native browser prompts replaced with shared accessible modals for notification delete, system-events clear, backup delete/restore, and movie-subtitle delete.
- **Isolation is respected.** The harness uses temporary SQLite/config/media/backup roots and a `BrowserAcceptanceMetadataProvider` so the suite does not touch developer data or probe the LAN.
- **TypeScript discipline is mixed.** Strict app/server TypeScript and the production Vite build pass, but `repository-wide tsc -b` is still blocked by pre-existing NodeNext/module-resolution diagnostics and should not be cited as green.
- **Bundle/runtime coupling improved.** The scheduler runtime was kept out of the browser bundle earlier in the day; static-serving revalidation ensures fresh SPA HTML after daemon refresh.

## Risks or Blockers

1. **Static-gate blockers remain unrelated to this track.** `ManualEpisodeMatchDialog.tsx:83`, `SystemTasksPage.tsx:151`, and `useUIStore.ts:15-22` currently block full-app static gates per the plan.
2. **Kimi acceptance is documented but not machine-verifiable by this review.** The plan records multiple connected-browser walkthroughs; the review relies on those assertions.
3. **Phase 5 release acceptance is open.** The complete browser suite, full regression, and independent live-browser walkthrough have not been recorded as finished.
4. **LAN scanning risk mitigated.** The `/api/indexers/detect` route now accepts injected discovery; the isolated fixture supplies an empty local implementation so browser tests do not scan the household network.
5. **Deterministic provider fakes are in place, but public-provider consultation was observed during early red runs.** The suite now uses the local `BrowserAcceptanceMetadataProvider`; ensure this remains enforced in CI.

## Recommended Next Actions

1. Resolve the three unrelated static-gate findings (`ManualEpisodeMatchDialog.tsx`, `SystemTasksPage.tsx`, `useUIStore.ts`) so the production build/typecheck can gate the whole app.
2. Complete the remaining durable workflow slices: full settings CRUD, collection operations, scheduler failure/recovery, and subtitle provider/search flows.
3. Record the final Phase 5 run: run the complete browser suite plus full regression, capture coverage boundaries, and archive only if every in-scope route/workflow has browser evidence.
4. Add a single documented entry command for the production browser suite and a CI step that runs it against disposable roots.
5. Convert the diagnostic performance samples into explicit, reproducible thresholds (e.g., p95 `DOMContentLoaded` and load budget per route) before claiming FR-5 complete.
