# Track Review: Filter Service Test Coverage

**Review date:** 2026-07-12
**Reviewer:** Measure daily automation
**Commits reviewed:** `f863f614` — docs(measure): add daily review reports 2026-07-11 for changed tracks

---

## Summary of changes

No new implementation work in the last 24 hours. `f863f614` only added the previous daily review report (`review-2026-07-11.md`). No source files, tests, specs, or plans were modified.

`FilterService.test.ts` exists in `server/src/services/` with 26 passing tests covering CRUD, validation, `applyToSeries`, and `applyToIndexers`.

## Spec alignment

**Verdict: partial**

- **Misaligned premise:** The spec still claims `FilterService` has "no dedicated unit-test coverage." This is incorrect; a dedicated test file exists and passes.
- **Partially met:** Existing tests cover positive/negative matches, composite AND/OR logic, and several validation paths.
- **Coverage gap:** Branch coverage for `FilterService.ts` is **68.48%**, below the ≥80% target. Statement coverage is 77.83% and line coverage is 78.26%.
- **Misaligned scope:** The acceptance criteria still reference quality, language, size, indexer, and custom-format filter paths. The current `FilterService` source does not implement those evaluations; its fields are `monitored`, `network`, `genre`, `tag`, `rating`, `status` (series) and `protocol`, `enabled`, `capability`, `priority`, `tag` (indexers).

## Code quality observations

- No new code to review.
- Existing `FilterService.test.ts` follows the project’s `vi.hoisted()` + `vi.mock(...)` pattern.
- The plan still assumes a green-field TDD approach, which is inappropriate for an already-tested service.

## Risks or blockers

| Risk | Impact | Likelihood |
|---|---|---|
| Branch coverage (68.48%) remains below the 80% target | Medium | Certain |
| Acceptance criteria describe non-existent filter fields | High | High |
| Plan duplicates existing CRUD and evaluation tests | Medium | High |
| Spec premise discourages targeted gap-filling | Medium | High |

## Recommended next actions

1. Do not execute the current plan as written.
2. Update the spec to acknowledge the existing `FilterService.test.ts` and remove references to quality/language/size/custom-format unless those are planned as separate features.
3. Add targeted tests for the uncovered branches in `FilterService.ts` (uncovered lines include 68, 72, 83-89, 98-102, 114, 117, 125, 130, 137, 145, 154, 159, 175, 190-194, 202, 206, 210, 221, 243-246, 254, 258, 261, 266-270, 291, 299, 318, 327, 331, 335).
4. Re-run coverage after adding tests and verify branch coverage ≥80%.
5. Once coverage target is met, archive the track with documentation updates to `measure/tech-debt.md` and `measure/tracks.md`.
